<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shop.dao.ProductDao">

    <resultMap id="productMap" type="Product">
        <id property="id" column="id"/>
        <result property="categoryId" column="category_id"/>
        <result property="categoryName" column="category_name"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="selectById" parameterType="int" resultMap="productMap">
        SELECT p.*, c.name AS category_name
        FROM product p
        LEFT JOIN category c ON p.category_id = c.id
        WHERE p.id = #{id}
    </select>

    <select id="selectAll" resultMap="productMap">
        SELECT p.*, c.name AS category_name
        FROM product p
        LEFT JOIN category c ON p.category_id = c.id
        WHERE p.status = 1
        ORDER BY p.create_time DESC
    </select>

    <select id="selectByCategoryId" parameterType="int" resultMap="productMap">
        SELECT p.*, c.name AS category_name
        FROM product p
        LEFT JOIN category c ON p.category_id = c.id
        WHERE p.category_id = #{categoryId} AND p.status = 1
        ORDER BY p.create_time DESC
    </select>

    <select id="selectByKeyword" parameterType="string" resultMap="productMap">
        SELECT p.*, c.name AS category_name
        FROM product p
        LEFT JOIN category c ON p.category_id = c.id
        WHERE p.status = 1 AND p.name LIKE CONCAT('%', #{keyword}, '%')
        ORDER BY p.create_time DESC
    </select>

    <select id="selectWithCategory" resultMap="productMap">
        SELECT p.*, c.name AS category_name
        FROM product p
        LEFT JOIN category c ON p.category_id = c.id
        <where>
            <if test="name != null and name != ''">
                AND p.name LIKE CONCAT('%', #{name}, '%')
            </if>
            <if test="categoryId != null and categoryId != 0">
                AND p.category_id = #{categoryId}
            </if>
        </where>
        ORDER BY p.create_time DESC
    </select>

    <insert id="insert" parameterType="Product" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO product (name, description, price, stock, image, category_id, status)
        VALUES (#{name}, #{description}, #{price}, #{stock}, #{image}, #{categoryId}, #{status})
    </insert>

    <update id="update" parameterType="Product">
        UPDATE product
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="description != null">description = #{description},</if>
            <if test="price != null">price = #{price},</if>
            <if test="stock != null">stock = #{stock},</if>
            <if test="image != null">image = #{image},</if>
            <if test="categoryId != null">category_id = #{categoryId},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="int">
        DELETE FROM product WHERE id = #{id}
    </delete>

    <update id="updateStock">
        UPDATE product SET stock = stock - #{quantity}, sales = sales + #{quantity}
        WHERE id = #{id} AND stock >= #{quantity}
    </update>
</mapper>
