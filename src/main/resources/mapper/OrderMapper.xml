<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shop.dao.OrderDao">

    <resultMap id="orderMap" type="Order">
        <id property="id" column="id"/>
        <result property="orderNo" column="order_no"/>
        <result property="userId" column="user_id"/>
        <result property="totalPrice" column="total_price"/>
        <result property="receiverName" column="receiver_name"/>
        <result property="receiverPhone" column="receiver_phone"/>
        <result property="receiverAddress" column="receiver_address"/>
        <result property="createTime" column="create_time"/>
        <result property="payTime" column="pay_time"/>
    </resultMap>

    <resultMap id="orderWithUserMap" type="Order" extends="orderMap">
        <result property="username" column="username"/>
    </resultMap>

    <select id="selectById" parameterType="int" resultMap="orderMap">
        SELECT * FROM `order` WHERE id = #{id}
    </select>

    <select id="selectByOrderNo" parameterType="string" resultMap="orderMap">
        SELECT * FROM `order` WHERE order_no = #{orderNo}
    </select>

    <select id="selectByUserId" parameterType="int" resultMap="orderWithUserMap">
        SELECT o.*, u.username
        FROM `order` o
        LEFT JOIN user u ON o.user_id = u.id
        WHERE o.user_id = #{userId}
        ORDER BY o.create_time DESC
    </select>

    <select id="selectAll" resultMap="orderWithUserMap">
        SELECT o.*, u.username
        FROM `order` o
        LEFT JOIN user u ON o.user_id = u.id
        ORDER BY o.create_time DESC
    </select>

    <insert id="insert" parameterType="Order" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO `order` (order_no, user_id, total_price, status, receiver_name, receiver_phone, receiver_address)
        VALUES (#{orderNo}, #{userId}, #{totalPrice}, #{status}, #{receiverName}, #{receiverPhone}, #{receiverAddress})
    </insert>

    <update id="updateStatus" parameterType="Order">
        UPDATE `order` SET status = #{status}
        <if test="payTime != null">, pay_time = #{payTime}</if>
        WHERE id = #{id}
    </update>
</mapper>
