<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shop.dao.CartDao">

    <resultMap id="cartMap" type="Cart">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="productId" column="product_id"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <resultMap id="cartWithProductMap" type="Cart" extends="cartMap">
        <result property="productName" column="product_name"/>
        <result property="productPrice" column="product_price"/>
        <result property="productImage" column="product_image"/>
    </resultMap>

    <select id="selectByUserId" parameterType="int" resultMap="cartMap">
        SELECT * FROM cart WHERE user_id = #{userId}
    </select>

    <select id="selectByUserIdAndProductId" resultMap="cartMap">
        SELECT * FROM cart WHERE user_id = #{userId} AND product_id = #{productId}
    </select>

    <select id="selectWithProduct" parameterType="int" resultMap="cartWithProductMap">
        SELECT c.*, p.name AS product_name, p.price AS product_price, p.image AS product_image
        FROM cart c
        LEFT JOIN product p ON c.product_id = p.id
        WHERE c.user_id = #{userId}
    </select>

    <insert id="insert" parameterType="Cart" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO cart (user_id, product_id, quantity)
        VALUES (#{userId}, #{productId}, #{quantity})
    </insert>

    <update id="updateQuantity">
        UPDATE cart SET quantity = #{quantity} WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="int">
        DELETE FROM cart WHERE id = #{id}
    </delete>

    <delete id="deleteByUserId" parameterType="int">
        DELETE FROM cart WHERE user_id = #{userId}
    </delete>
</mapper>
